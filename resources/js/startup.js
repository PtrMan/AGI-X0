
function loaded() {
	entryScriptingAccessor.setEnvironmentSetupFunction("environmentSetupFunction");
}

function environmentSetupFunction() {
	// we do nothing for now because we just do a boring test
}
